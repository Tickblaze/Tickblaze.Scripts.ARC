<Window
    mc:Ignorable="d"
	x:Class="Tickblaze.Test.Wpf.MainWindow"
	Title="MainWindow" Height="450" Width="800"
	xmlns:arc="clr-namespace:Tickblaze.Test.Wpf"
    xmlns:local="clr-namespace:Tickblaze.Test.Wpf"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
	xmlns:common="clr-namespace:Tickblaze.Scripts.Arc.Common;assembly=Tickblaze.Scripts.Arc.Common">
	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<x:Static Member="common:ResourceDictionaries.DefaultResources" />
			</ResourceDictionary.MergedDictionaries>

			<Style x:Key="DefaultTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
				<Setter Property="Foreground" Value="Maroon" />
			</Style>
		</ResourceDictionary>
	</Window.Resources>
	<DockPanel>
		<Menu>
			<MenuItem
				BorderThickness="2"
				Background="White"
				BorderBrush="Maroon"
				Grid.IsSharedSizeScope="True">
				<MenuItem.Header>
					<TextBlock
						Text="ASDS"
						FontWeight="Bold"
						Foreground="Maroon"
						FontSize="13" />
				</MenuItem.Header>
				<MenuItem.Resources>
					<Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
						<Setter Property="Foreground" Value="White" />
					</Style>
				</MenuItem.Resources>
				<MenuItem Header="ASDSD" />
				<common:ToggleButtonMenuItem HeaderText="ASDSDAAA" />
				<common:ToggleButtonMenuItem HeaderText="ASDSDAAA" />
			</MenuItem>
			<MenuItem Header="ASDSD" />
		</Menu>
		
		
		<!--<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<ToggleButton x:Name="SettingsButton" Content="FVG">
			</ToggleButton>

			<Popup
				Placement="Bottom"
				PlacementTarget="{Binding Source={x:Reference SettingsButton}}"
				IsOpen="{Binding IsChecked, Source={x:Reference SettingsButton}}">
				<Grid Background="White">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					
					<Grid.ColumnDefinitions>
						<ColumnDefinition MinWidth="200" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>

					<TextBlock Grid.Row="0" Grid.Column="0" Text="Show Fresh Gaps" />
					<CheckBox Grid.Row="0" Grid.Column="1" IsChecked="True" />

					<TextBlock Grid.Row="0" Grid.Column="0" Text="Show Tested Gaps" />
					<mah:NumericUpDown Grid.Row="0" Grid.Column="1" NumericInputMode="Numbers" />
				</Grid>
			</Popup>
		</Grid>-->
	</DockPanel>
</Window>
